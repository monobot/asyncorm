queryset:
    really as django querysets that is generator
    add exclude

    allow nested behaviour ¿?

    ordering:
        in meta and add it to all queries
        add ordering to methods when nested behaviour implemented

